# API_REST (Node.js + Vue 3)

Projet organisé avec :
- `api/` : serveur **Node + Express**
- `client/` : application **Vue 3 (Vite)**

## Installation
```bash
# Backend
cd api
cp .env.example .env
npm install
npm run dev   

# Frontend
cd ../client
npm install
npm run dev   
